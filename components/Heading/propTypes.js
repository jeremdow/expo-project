import PropTypes from 'prop-types';

const HeadingPropTypes = {
  variant: PropTypes.string.isRequired,
  component: PropTypes.string,
  children: PropTypes.string.isRequired,
};

export default HeadingPropTypes;
